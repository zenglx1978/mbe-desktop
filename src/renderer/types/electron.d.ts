/**
 * Electron preload / window.electronAPI 的全局类型定义在 env.d.ts：
 * interface ElectronAPI、ElectronObserver、ElectronPattern、Window.electronAPI。
 *
 * 本文件保留为空模块占位，避免曾在此处的「带 export {} 的 interface ElectronAPI」
 * 与全局声明重复且无法合并的问题。
 */
export {}
